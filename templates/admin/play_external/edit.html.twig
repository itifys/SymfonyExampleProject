{% extends 'adminbase.html.twig' %}

{% block title %}Edit PlayExternal{% endblock %}

{% block body %}
    <h1>Edit PlayExternal</h1>

    {{ include('admin/play_external/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('play_external_index') }}" class="btn btn-primary">back to list</a>
{#    {% for photo in photos %}#}
{#        <img src="{{ asset(photo.photo) }}" style="height: 150px">#}
{#    {% endfor %}#}

    {{ include('admin/play_external/_delete_form.html.twig') }}
{% endblock %}
