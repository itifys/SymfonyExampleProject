<form method="post" action="{{ path('questionaire_part_delete', {'id': questionaire_part.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ questionaire_part.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
