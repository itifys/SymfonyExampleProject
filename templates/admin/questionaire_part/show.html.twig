{% extends 'adminbase.html.twig' %}

{% block title %}QuestionairePart{% endblock %}

{% block body %}
    <h1>QuestionairePart</h1>
{#    {% for tag in questionaire_part.tags %}#}
{#        {{ tag.tag }}#}
{#    {% endfor %}#}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ questionaire_part.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{ questionaire_part.image ? asset(questionaire_part.image) : '' }}" style="height: 200px"></td>
            </tr>
            <tr>
                <th>Tags</th>
                <td>
                    {% for tag in tags %}
                        <div class="tags">{{ tag.tag }}</div>
                    {% endfor %}
                </td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ questionaire_part.createdAt ? questionaire_part.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ questionaire_part.updatedAt ? questionaire_part.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('questionaire_part_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('questionaire_part_edit', {'id': questionaire_part.id}) }}" class ="btn btn-warning">edit</a>

    {{ include('admin/questionaire_part/_delete_form.html.twig') }}
{% endblock %}
