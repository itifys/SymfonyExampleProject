{% extends 'base.html.twig' %}

{% block title %}QuestionairePart{% endblock %}

{% block body %}
    <h1>QuestionairePart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ questionaire_part.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ questionaire_part.image }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ questionaire_part.createdAt ? questionaire_part.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ questionaire_part.updatedAt ? questionaire_part.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ questionaire_part.title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ questionaire_part.text }}</td>
            </tr>
            <tr>
                <th>Display_type</th>
                <td>{{ questionaire_part.displayType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('questionaire_part_index') }}">back to list</a>

    <a href="{{ path('questionaire_part_edit', {'id': questionaire_part.id}) }}">edit</a>

    {{ include('questionaire_part/_delete_form.html.twig') }}
{% endblock %}
