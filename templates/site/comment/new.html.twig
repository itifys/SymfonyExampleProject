{% extends 'base.html.twig' %}

{% block title %}New Comment{% endblock %}

{% block body %}
    <h1>Create new Comment</h1>

    {{ include('site/comment/_form.html.twig') }}

    <a href="{{ path('comment_index') }}" class="btn btn-primary">back to list</a>
{% endblock %}
